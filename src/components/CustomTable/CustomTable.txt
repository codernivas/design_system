type User = {
  id: number
  name: string
  email: string
}
  const userData: User[] = [
    { id: 1, name: "Nivas Jerry", email: "nivas@example.com" },
    { id: 2, name: "Jane Doe", email: "jane@example.com" },
    { id: 3, name: "Jane Doe", email: "jane@example.com" },
    { id: 4, name: "Jane Doe", email: "jane@example.com" },
  ]

  --------return--------
 <CustomTable
        data={userData}
        columns={[
          { header: "ID", accessor: "id" },
          { header: "Name", accessor: "name" },
          { header: "Email", accessor: "email" },
        ]}
        onRowClick={(row) => console.log("Row clicked:", row)}
        actions={(row) => (
          <div
            style={{
              flexDirection: "row",
              display: "flex",
              justifyContent: "space-between",
            }}
          >
            <CustomButton
              title="Edit"
              onPress={() => alert(`Edit ${row.name}`)}
            />
            <button onClick={() => alert(`Delete ${row.name}`)}>Delete</button>
          </div>
        )}
      />